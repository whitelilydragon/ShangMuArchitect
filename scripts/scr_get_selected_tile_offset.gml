var op = obj_level_editor;

var width  = background_get_width(op.user_tile_selected);
var height = background_get_height(op.user_tile_selected);

op.user_grid_offset_x = -abs(floor(width / 8))

op.user_grid_offset_y = -abs(floor(width / 8))
